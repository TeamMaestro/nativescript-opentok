<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="pageLoaded">
  <StackLayout id="publisherContainer" orientation="vertical">
    <Label text="OpenTok" class="message" textWrap="true"/>
    <Button text="Toggle Video" tap="{{ toggleVideo }}" />
    <Button text="Toggle Audio" tap="{{ toggleAudio }}" />
    <Button text="Toggle Camera" tap="{{ toggleCamera }}" />
  </StackLayout>
</Page>